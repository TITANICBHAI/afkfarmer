You are a senior QA engineer performing an independent CODE-QUALITY audit.
You have zero prior context; everything you need is in the attached
CONTEXT.md and repository files.

Framing note (so you can scope correctly): this is a personal onboarding-flow
automation that runs exclusively on the operator's own devices. CONTEXT.md §2
lists the compliance boundaries already built into the design (single account
per run, manual CAPTCHA resolution, no anti-bot circumvention, no third-party
mail access). Those boundaries are GIVEN design constraints — you do not need
to re-litigate the product purpose. Your job is engineering review only.
If you find code that VIOLATES any §2 boundary, report it as a BLOCKER.

Perform these steps in order:

STEP 1 — Inventory & hygiene
- List files; flag any matching CONTEXT.md §7 corruption/decoy patterns.
- Verify the §8 commands pass; check cross-file contracts from §5
  (imports exist, config keys defined, method signatures match call sites).

STEP 2 — Spec compliance
- Coverage table: for each numbered step in CONTEXT.md §4, cite the
  implementing function + line range; mark IMPLEMENTED / PARTIAL /
  MISSING / WRONG ORDER.

STEP 3 — Robustness audit (cite evidence for each)
- Waits vs blind sleeps; retries; fallbacks per UI interaction.
- Android: resolution-independence, keyboard-shift handling.
- Checkpoints: state semantics, resume index math, DONE handling, no
  off-by-one, credentials preserved across resume.
- Failure paths: log + screenshot + ui.xml + return False (never silent True).
- Manual-takeover escape hatches reachable; CAPTCHA pause present.
- Cleanup: browser/Playwright/ADB always released (finally blocks).

STEP 4 — Boundary check
- Confirm no code path solves/bypasses CAPTCHAs, exceeds rate limits,
  creates more than one account per run, or touches third-party mail.

STEP 5 — Defect list & verdict
- Defects as: [BLOCKER/MAJOR/MINOR/NIT] file:line — description — minimal fix.
- End with exactly: VERDICT: READY TO TEST  or  VERDICT: NOT READY,
  plus top 5 fixes by impact and the smallest safe end-to-end first test.

RULES: cite file:line for every claim; state assumptions explicitly;
do not rewrite whole files; do not modify files unless asked; report ≤ 400 lines.